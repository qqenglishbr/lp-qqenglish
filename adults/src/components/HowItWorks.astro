---
// HowItWorks - 3 passos simples para começar
const steps = [
  {
    number: "1",
    title: "Agende sua primeira aula",
    description: "Preencha o formulário e escolha o melhor horário para sua aula experimental",
    icon: `<svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/><path d="M8 14h.01M12 14h.01M16 14h.01M8 18h.01M12 18h.01"/></svg>`
  },
  {
    number: "2",
    title: "Conheça seu professor",
    description: "25 minutos de conversação ao vivo com um professor certificado TESOL",
    icon: `<svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M17 21v-2a4 4 0 00-4-4H5a4 4 0 00-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 00-3-3.87"/><path d="M16 3.13a4 4 0 010 7.75"/></svg>`
  },
  {
    number: "3",
    title: "Comece a evoluir",
    description: "Escolha seu plano e pratique conversação todo dia com professores dedicados",
    icon: `<svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M18 20V10"/><path d="M12 20V4"/><path d="M6 20v-6"/></svg>`
  }
];
---

<section class="how-it-works" id="como-funciona">
  <div class="container">
    <div class="section-header reveal">
      <span class="section-badge">Como funciona</span>
      <h2>Três passos para começar a falar inglês</h2>
      <p>Simples, rápido e sem burocracia</p>
    </div>

    <div class="steps-grid">
      {steps.map((step, index) => (
        <div class="step-card reveal" style={`animation-delay: ${index * 0.15}s`}>
          <div class="step-number">{step.number}</div>
          <div class="step-icon" set:html={step.icon} />
          <h3>{step.title}</h3>
          <p>{step.description}</p>
          {index < steps.length - 1 && (
            <div class="step-connector">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M5 12h14M12 5l7 7-7 7"/>
              </svg>
            </div>
          )}
        </div>
      ))}
    </div>

    <div class="cta-wrapper reveal">
      <a href="#lead-form" class="btn-lp">
        Quero Experimentar
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M5 12h14M12 5l7 7-7 7"/>
        </svg>
      </a>
    </div>
  </div>
</section>

<style>
  .how-it-works {
    padding: var(--section-padding) 0;
    background: linear-gradient(180deg, var(--color-gray-50) 0%, white 100%);
  }

  .section-header {
    text-align: center;
    max-width: 640px;
    margin: 0 auto 3rem;
  }

  .section-badge {
    display: inline-block;
    padding: 0.375rem 1rem;
    font-size: 0.8125rem;
    font-weight: 600;
    color: var(--color-primary);
    background: var(--color-primary-subtle);
    border-radius: var(--radius-full);
    margin-bottom: 1rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .section-header h2 {
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    margin-bottom: 0.75rem;
  }

  .section-header p {
    font-size: 1.125rem;
    color: var(--color-gray-600);
  }

  .steps-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
    margin-bottom: 3rem;
  }

  @media (min-width: 768px) {
    .steps-grid {
      grid-template-columns: repeat(3, 1fr);
      gap: 2rem;
    }
  }

  .step-card {
    position: relative;
    background: white;
    padding: 2rem 1.5rem;
    border-radius: var(--radius-xl);
    text-align: center;
    box-shadow: var(--shadow-sm);
    transition: all var(--transition-base);
  }

  .step-card:hover {
    box-shadow: var(--shadow-lg);
    transform: translateY(-4px);
  }

  .step-number {
    position: absolute;
    top: -12px;
    left: 50%;
    transform: translateX(-50%);
    width: 32px;
    height: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-display);
    font-size: 1rem;
    font-weight: 700;
    color: white;
    background: linear-gradient(135deg, var(--color-primary) 0%, #0e7490 100%);
    border-radius: 50%;
  }

  .step-icon {
    width: 72px;
    height: 72px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--color-primary-subtle);
    color: var(--color-primary);
    border-radius: var(--radius-lg);
    margin: 1rem auto 1.5rem;
    transition: all var(--transition-base);
  }

  .step-card:hover .step-icon {
    background: var(--color-primary);
    color: white;
  }

  .step-card h3 {
    font-size: 1.25rem;
    margin-bottom: 0.75rem;
  }

  .step-card p {
    font-size: 0.9375rem;
    color: var(--color-gray-600);
    line-height: 1.6;
  }

  .step-connector {
    display: none;
  }

  @media (min-width: 768px) {
    .step-connector {
      display: flex;
      position: absolute;
      right: -1.5rem;
      top: 50%;
      transform: translateY(-50%);
      color: var(--color-gray-300);
      z-index: 1;
    }
  }

  .cta-wrapper {
    text-align: center;
  }

  .reveal {
    opacity: 0;
    transform: translateY(20px);
    transition: all 0.6s ease;
  }

  .reveal.is-visible {
    opacity: 1;
    transform: translateY(0);
  }
</style>

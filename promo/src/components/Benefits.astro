---
// Benefits - Por que assinar agora
const benefits = [
  {
    icon: `<svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
      <path d="M5 13l4 4L19 7"/>
      <path d="M12 2a10 10 0 1010 10"/>
    </svg>`,
    title: "Acesso imediato",
    description: "Finalize o checkout e comece a agendar suas aulas no mesmo instante."
  },
  {
    icon: `<svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
      <circle cx="12" cy="12" r="10"/>
      <path d="M12 6v6l4 2"/>
    </svg>`,
    title: "Rotina flexível",
    description: "Aulas 24/7 para encaixar no seu dia, com total liberdade de horários."
  },
  {
    icon: `<svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
      <path d="M12 20V4"/>
      <path d="M6 10l6-6 6 6"/>
    </svg>`,
    title: "Evolução contínua",
    description: "Mantenha o ritmo da FTL e avance com prática constante de conversação."
  },
  {
    icon: `<svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
      <path d="M21 15a2 2 0 01-2 2H7l-4 4V5a2 2 0 012-2h14a2 2 0 012 2z"/>
    </svg>`,
    title: "Professores dedicados",
    description: "Conte com professores certificados para corrigir e guiar seu progresso."
  }
];
---

<section class="benefits" id="beneficios">
  <div class="container">
    <div class="section-header reveal">
      <span class="section-badge">Por que assinar agora?</span>
      <h2>Você já conhece a experiência. Agora é hora de avançar.</h2>
    </div>

    <div class="benefits-grid">
      {benefits.map((benefit, index) => (
        <div class="benefit-card reveal" style={`animation-delay: ${index * 0.1}s`}>
          <div class="benefit-icon" set:html={benefit.icon} />
          <h3>{benefit.title}</h3>
          <p>{benefit.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .benefits {
    padding: var(--section-padding) 0;
    background: white;
  }

  .section-header {
    text-align: center;
    max-width: 720px;
    margin: 0 auto 3rem;
  }

  .section-badge {
    display: inline-block;
    padding: 0.375rem 1rem;
    font-size: 0.8125rem;
    font-weight: 600;
    color: var(--color-promo-cta);
    background: rgba(37, 99, 235, 0.12);
    border-radius: var(--radius-full);
    margin-bottom: 1rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .section-header h2 {
    font-size: clamp(1.75rem, 4vw, 2.5rem);
  }

  .benefits-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 1.5rem;
  }

  @media (min-width: 768px) {
    .benefits-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .benefits-grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  .benefit-card {
    background: var(--color-gray-50);
    padding: 2rem 1.5rem;
    border-radius: var(--radius-lg);
    text-align: center;
    transition: all var(--transition-base);
    border: 1px solid transparent;
  }

  .benefit-card:hover {
    background: white;
    border-color: var(--color-gray-200);
    box-shadow: var(--shadow-md);
    transform: translateY(-4px);
  }

  .benefit-icon {
    width: 64px;
    height: 64px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, var(--color-promo-cta) 0%, #1d4ed8 100%);
    color: white;
    border-radius: var(--radius-lg);
    margin: 0 auto 1.25rem;
  }

  .benefit-card h3 {
    font-size: 1.125rem;
    margin-bottom: 0.75rem;
    color: var(--color-dark);
  }

  .benefit-card p {
    font-size: 0.9375rem;
    color: var(--color-gray-600);
    line-height: 1.6;
  }

  .benefit-card:hover .benefit-icon {
    transform: scale(1.08);
    transition: transform var(--transition-base);
  }
</style>
